# Base URL del backend NestJS RE|CARS (senza slash finale).
# In sviluppo locale il backend gira su http://localhost:3000 (pnpm start:dev in Progetto/Backend).
# In produzione impostare l'URL pubblico del backend (es. https://api.recars.example.com).
NEXT_PUBLIC_API_URL=http://localhost:3000
